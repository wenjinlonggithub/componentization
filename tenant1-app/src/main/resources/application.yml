tenant:
  id: tenant1
  name: "高级租户应用"

spring:
  application:
    name: tenant1-application
  profiles:
    active: tenant1

server:
  port: 8081

logging:
  level:
    com.company: DEBUG
    root: INFO